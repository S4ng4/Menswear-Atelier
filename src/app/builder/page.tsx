import { OutfitBuilder } from "@/components/builder/OutfitBuilder";

export const metadata = {
  title: "Outfit Builder — Atelier",
  description: "Compose timeless menswear combinations with live elegance scoring.",
};

export default function BuilderPage() {
  return <OutfitBuilder />;
}
